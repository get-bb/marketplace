Search MCP connections across Claude Code, Codex, Gemini CLI, OpenCode, and Cursor. Filter by harness or tag, and expand a row to edit tags.

Reconnect through the installed harness or remove a configuration after confirmation. Removal saves a backup. Project and user configurations stay separate.

Claude Code reports connection status; Codex reports saved credentials. Other harnesses need native status checks. Requires BB 0.43+ and an enrolled host. Linux browser sign-in also needs util-linux `script`.
