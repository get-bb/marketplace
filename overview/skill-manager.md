Keep one copy of each skill in a shared hub, then link or copy it into your agents. See missing skills, changed copies, and available updates.

Search skills.sh or install from a GitHub repository. Choose which agents receive each skill, compare differences, and organize the list with tags.

The same actions are available through `bb skill-manager`. Requires BB 0.43+. Downloads and update checks use Git and internet access.
