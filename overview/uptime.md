## Website monitoring

Add sites to monitor from the BB server. See availability, response times and a history of the last 60 checks per site. The sidebar indicates when a site is down.

## Requirements and data

The BB server must be running and able to reach the configured sites. Checks make HTTP requests to those sites. The plugin starts empty; addresses and check history stay in the plugin storage. No monitoring service account is required.
