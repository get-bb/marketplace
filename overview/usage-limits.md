## See your remaining quota

Keep provider usage limits visible in the sidebar. Open the details to see available quota windows and reset times across connected hosts. Refresh on demand or use `bb usage-limits` from a terminal.

## Requirements and data

The plugin uses BB provider integrations and their existing authentication. Results depend on the provider and connected host reporting quota information. No credentials or personal usage samples are bundled. Results are cached locally for five minutes by default.
