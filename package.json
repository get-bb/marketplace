{
  "name": "bb-marketplace-registry",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:stats": "node scripts/build-stats.mjs",
    "check": "node scripts/build.mjs --liveness"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
